name: Release

on: [push, pull_request]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: '17.0.7'
          distribution: graalvm
          github-token: ${{ secrets.GITHUB_TOKEN }}
          native-image-job-reports: 'true'

      - name: Update version
        run: mvn versions:set -DnewVersion=${GITHUB_REF#refs/tags/}

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Build native image
        run: native-image --no-server -jar target/jeditor-*-jar-with-dependencies.jar

      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          name: jeditor-${GITHUB_REF#refs/tags/}-${{ matrix.os }}-x86_64.exe
          path: target/jeditor-${GITHUB_REF#refs/tags/}-${{ matrix.os }}-x86_64.exe

#      - name: Create Release
#        uses: softprops/action-gh-release@v1
#        if: startsWith(github.ref, 'refs/tags/')
#        with:
#          files: |
#            target/jeditor-${GITHUB_REF#refs/tags/}-jar-with-dependencies.jar
#            target/jeditor-${GITHUB_REF#refs/tags/}-${{ matrix.os }}-x86_64